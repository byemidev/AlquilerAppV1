@model IEnumerable<AlquileresMVC.Models.user.carDescription>

<div class="card">
    <div class="card-header">
    </div>
    <div class="card-body">
        @foreach (var item in Model)
        {
            <div class="mb-3">
                <label class="form-label">@item.titulo</label>
            </div>
        }
    </div>
</div>